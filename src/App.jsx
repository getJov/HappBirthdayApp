export default function App() {
  return (
    <main className="app-shell">
      <section className="intro-panel">
        <p className="eyebrow">Frontend-only greeting builder</p>
        <h1>Happy Birthday</h1>
        <p>
          Build a personalized birthday page, encode it into a shareable link,
          and open it without a backend or database.
        </p>
      </section>
    </main>
  );
}
